# MySQL CSV Importer

This is a Python project that connects to a MySQL database, creates a database and table if they do not exist, and imports user data from a CSV file. It prevents duplicate entries by checking for existing `user_id`s before inserting new records.
